import 'package:flutter/material.dart';

class AddCasePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Add Case Details"),
      ),
      body: Center(
        child: Text("Add Case Form Coming Soon"),
      ),
    );
  }
}